@model DirectoryListApp.Models.DirectoryModel

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<link href="~/Scripts/DataTable/datatables.min.css" rel="stylesheet" />
<script src="~/Scripts/DataTable/datatables.min.js"></script>
<br />
<div class="row">
    <div class="span4">
        <h4>Directory List</h4>
    </div>

    <div class="pull-right">
        <a class="btn btn-primary" href="@Url.Action("Create", "DirectoryCategory")">
            Create`New
        </a>
    </div>


</div>
<br />
<div class="well well-small">
    @foreach (var item in Model.DirectoryModelList)
    {
      <div>
          <ul>
              <li>
                  <p>@item.DirectoryItemName</p>
                  <p>@item.PermanentAddress</p>
                  <a class="details" href="~/DirectoryItem/Details/@item.DirectoryItemId" title="विवरण हेर्नुहोस्"><i class="fa fa-pinterest" style="color:lightseagreen;" data-toggle="modal" data-target="#myModal"></i></a>
                  <a href="/DirectoryItem/Edit/@item.DirectoryItemId " title="Edit"><i class="fa fa-edit" style="color:blue;"></i></a>
              </li>
          </ul>
      </div>
    }
    @*@if (Model.DirectoryModelList.Count > 0)
    {
        int Sno = ViewBag.currentPage == 1 ? 0 : (ViewBag.currentPage - 1) * 20;
        <table id="ItemCategory" class="table table-bordered table-striped table-condensed">
            <thead>
                <tr>
                    <th class="span1"><label>S.N</label> </th>
                    <th> @Html.LabelFor(model => model.DirectoryItemName)</th>

                    <th></th>

                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.DirectoryModelList)
                {

                    <tr class="@(Sno++ % 2 == 0 ? "success" : "warning")">
                        <td class="span1l">
                            @Sno.ToString()
                        </td>
                        <td>@item.DirectoryItemName</td>

                        <td>
                            <a href="/DirectoryItem/Edit/@item.DirectoryItemId " title="Edit"><i class="fa fa-edit" style="color:blue;"></i></a>
                            <a class="details" href="~/DirectoryItem/Details/@item.DirectoryItemId" title="विवरण हेर्नुहोस्"><i class="fa fa-pinterest" style="color:black;" data-toggle="modal" data-target="#myModal"></i></a>
                        </td>


                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <h4>No records found!</h4>
    }*@

</div>
<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title" id="myModalLabel">Directory Details</h4>
            </div>
            <div id="ModalBody" class="modal-body">
                <form></form>
            </div>
        </div>
    </div>
</div>
<script>
    var table = $('#ItemCategory').DataTable({
        "order": false,
        "pageLength": 25,
        "language": {
            "lengthMenu": "Display _MENU_ records per page",
            "zeroRecords": "No records available!",
            "info": " पृष्ठ _PAGE_ को _PAGES_",
            "infoEmpty": "No records available",
            "infoFiltered": "(filtered from _MAX_ total records)",
            "paginate":
            {
                "previous": "Previous",
                "next": "Next"
            }
        }
    });
</script>
<script>
    $(".details").on("click", function () {
        debugger;
        var href = $(this).attr('href');
        $.ajax({
            url: href,
            success: function (result) {
                debugger;
                if (result) {
                    $("#myModal").modal('show');
                    $('#ModalBody').html(result);
                }

            },
            error: function (xhr, status, error) {
                alert("Error");
            }
        });

    });
</script>

